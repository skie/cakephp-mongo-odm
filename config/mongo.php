<?php
declare(strict_types=1);

/**
 * Mongo Plugin Configuration
 *
 * Host applications copy settings into `config/mongo.php` and load via
 * `Configure::load('mongo')`. Keys are read as `Configure::read('Mongo.*')`.
 */
return [
    'Mongo' => [
    ],
];
