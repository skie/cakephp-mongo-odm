{#
/**
 * Crustum Mongo ODM — Controller view action.
 */
#}
    /**
     * View method
     *
     * @param string|null $id {{ singularHumanName }} _id.
     * @return \Cake\Http\Response|null|void Renders view
     * @throws \Cake\Datasource\Exception\RecordNotFoundException When record not found.
     */
    public function view($id = null)
    {
{% set allAssociations = associations.belongsTo|merge(associations.hasOne)|merge(associations.hasMany)|merge(associations.belongsToMany) %}
        ${{ singularName }} = $this->{{ currentModelName }}->get($id, contain: {{ Bake.exportArray(allAssociations)|raw }});
        $this->set(compact('{{ singularName }}'));
    }


